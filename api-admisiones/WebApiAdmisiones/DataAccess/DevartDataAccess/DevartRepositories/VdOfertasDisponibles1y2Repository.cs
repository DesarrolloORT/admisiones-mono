//------------------------------------------------------------------------------
// This code was generated by Entity Developer tool using the template for generating Repositories and a Unit of Work for EF Core model.
// Code is generated on: 21/08/2026 14:27:16
//
//------------------------------------------------------------------------------
using System;
using System.Linq;
using System.Collections.Generic;
using Microsoft.EntityFrameworkCore;
using BusinessLogic.IDevartRepositories;

namespace DataAccess.DevartRepositories
{
    public partial class VdOfertasDisponibles1y2Repository
    {
        /// <summary>
        /// Ofertas del producto y proceso que la persona todavia puede elegir: excluye las ofertas
        /// donde ya tiene una inscripcion vigente (sin baja).
        /// </summary>
        public virtual ICollection<BusinessLogic.Entities.VdOfertasDisponibles1y2> GetOfertasDisponibles(
            long idProducto,
            long idProceso,
            long codigoPersona)
        {
            var ctx = Context;

            return objectSet
                .Where(x => x.IdProducto == idProducto
                    // ID_PROCESO es NUMBER sin escala: mismo cast explicito que VdInscripcionesFresco*.
                    && x.IdProceso == (decimal)idProceso
                    && !ctx.Inscriptos.Any(i => i.IdOferta == x.IdOferta
                        && i.CodigoPersona == codigoPersona
                        && i.BajaInscr == null))
                .OrderBy(x => x.IdTurno)
                .ThenBy(x => x.IdOferta)
                .ToList()
                // La vista trae una fila por paquete: para turnos solo interesa la oferta.
                .GroupBy(x => x.IdOferta)
                .Select(g => g.First())
                .ToList();
        }
    }
}
