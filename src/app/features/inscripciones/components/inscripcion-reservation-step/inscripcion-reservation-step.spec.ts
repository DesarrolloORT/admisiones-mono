import { InscripcionReservationStep } from 'inscripcion-reservation-step';

describe('InscripcionReservationStep', () => {
  it('should be importable', () => {
    expect(InscripcionReservationStep).toBeDefined();
  });
});
