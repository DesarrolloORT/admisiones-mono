import type { BecaEstado } from 'mi-beca';

describe('BecaEstado', () => {
  it('should be importable as a type', () => {
    const value: BecaEstado | undefined = undefined;

    expect(value).toBeUndefined();
  });
});
