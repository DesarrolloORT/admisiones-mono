import { DashboardCardSummary } from './dashboard-card-summary';

describe('DashboardCardSummary', () => {
  it('should be importable', () => {
    expect(DashboardCardSummary).toBeDefined();
  });
});
