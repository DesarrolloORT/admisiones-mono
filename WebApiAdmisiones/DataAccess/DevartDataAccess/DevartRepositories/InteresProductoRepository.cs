//------------------------------------------------------------------------------
// This code was generated by Entity Developer tool using the template for generating Repositories and a Unit of Work for EF Core model.
// Code is generated on: 02/03/2026 12:08:20
//
//------------------------------------------------------------------------------
using Microsoft.EntityFrameworkCore;

namespace DataAccess.DevartRepositories
{
    public partial class InteresProductoRepository
    {
        public virtual ICollection<BusinessLogic.Entities.InteresProducto> GetByInteresIds(IEnumerable<decimal> ids)
        {
            var interesIds = ids.Distinct().ToList();
            if (interesIds.Count == 0)
            {
                return new List<BusinessLogic.Entities.InteresProducto>();
            }

            return objectSet
                .Where(ip => interesIds.Contains(ip.IdInteres))
                .ToList();
        }

        public virtual void ResetearGradosPorIntereses(IEnumerable<decimal> ids, decimal nuevoGrado)
        {
            var interesIds = ids.Distinct().ToList();
            if (interesIds.Count == 0)
            {
                return;
            }

            var productos = objectSet
                .Where(ip => interesIds.Contains(ip.IdInteres))
                .ToList();

            foreach (var producto in productos)
            {
                if (producto.IdGradoInteres != 5m)
                {
                    producto.IdGradoInteresAnt = producto.IdGradoInteres;
                    producto.IdGradoInteres = nuevoGrado;
                    producto.FechaModifInteresProd = DateTime.Today;
                }
            }
        }
    }
}
