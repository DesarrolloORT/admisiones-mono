//------------------------------------------------------------------------------
// This code was generated by Entity Developer tool using the template for generating Repositories and a Unit of Work for EF Core model.
// Code is generated on: 02/03/2026 12:08:20
//
//------------------------------------------------------------------------------
using System;
using System.Linq;
using System.Collections.Generic;
using Microsoft.EntityFrameworkCore;

namespace DataAccess.DevartRepositories
{
    public partial class EncuestaIniAdmisionRepository
    {
        /// <summary>
        /// Devuelve el último dato de pre-inscripción de la persona.
        /// </summary>
        public virtual BusinessLogic.Entities.EncuestaIniAdmision GetByPersona(long codigoPersona)
        {
            return objectSet
                .Where(e => e.CodigoPersona == codigoPersona)
                .Include(e => e.Proceso)
                .Include(e => e.Turno)
                .Include(e => e.Comienzo)
                .Include(e => e.Producto)
                .OrderByDescending(e => e.FechaIngreso)
                .FirstOrDefault();
        }

        public virtual BusinessLogic.Entities.EncuestaIniAdmision GetByPersonaProductoComienzo(
            long codigoPersona,
            long idProducto,
            long idComienzo)
        {
            return objectSet
                .Where(e => e.CodigoPersona == codigoPersona
                         && e.IdProducto == idProducto
                         && e.IdComienzo == idComienzo)
                .OrderByDescending(e => e.FechaIngreso)
                .FirstOrDefault();
        }

        public virtual bool ExisteCompletaPorDocumento(string tipoDocumento, string documento)
        {
            var normalizedTipoDocumento = tipoDocumento?.Trim() ?? string.Empty;
            var normalizedDocumento = documento?.Trim() ?? string.Empty;

            return objectSet.Count(e =>
                e.TipoDocumento != null
                && e.Documento != null
                && e.TipoDocumento.Trim() == normalizedTipoDocumento
                && e.Documento.Trim() == normalizedDocumento
                && e.EstadoEncuestaIniAdmision == "COMPLETA") > 0;
        }

    }
}
