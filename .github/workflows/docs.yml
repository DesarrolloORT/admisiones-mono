name: Frontend documentation

on:
  pull_request:
    branches:
      - main
    paths:
      - 'docs-site/**'
      - 'docs/**'
      - 'README.md'
      - 'CONTRIBUTING.md'
      - 'src/app/features/inscriptions/*.md'
      - 'src/app/features/becas/**/*.md'
      - '.github/workflows/docs.yml'
  push:
    branches:
      - 'v*.*.*/main'
    paths:
      - 'docs-site/**'
      - 'docs/**'
      - 'README.md'
      - 'CONTRIBUTING.md'
      - 'src/app/features/inscriptions/*.md'
      - 'src/app/features/becas/**/*.md'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: frontend-docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build documentation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: docs-site/package-lock.json

      - name: Install dependencies
        run: npm ci --prefix docs-site

      - name: Build documentation
        run: npm run build --prefix docs-site

      - name: Upload documentation artifact
        uses: actions/upload-artifact@v7
        with:
          name: frontend-docs
          path: docs-site/build

  deploy:
    name: Deploy documentation
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    needs: build
    runs-on: development-server
    environment: desarrollo
    steps:
      - name: Download documentation artifact
        uses: actions/download-artifact@v8
        with:
          name: frontend-docs
          path: docs-site-build

      - name: Publish to IIS site directory
        shell: pwsh
        run: |
          $target = '${{ vars.DOCS_SERVER_SITE_PATH }}'
          if ([string]::IsNullOrWhiteSpace($target)) {
            throw 'DOCS_SERVER_SITE_PATH is required.'
          }

          $resolvedTarget = [System.IO.Path]::GetFullPath($target)
          $root = [System.IO.Path]::GetPathRoot($resolvedTarget)
          if ($resolvedTarget.TrimEnd('\') -eq $root.TrimEnd('\')) {
            throw 'DOCS_SERVER_SITE_PATH cannot be a filesystem root.'
          }
          if (-not (Test-Path -LiteralPath $resolvedTarget -PathType Container)) {
            throw "Documentation site directory does not exist: $resolvedTarget"
          }

          Get-ChildItem -LiteralPath $resolvedTarget -Force | Remove-Item -Recurse -Force
          Copy-Item -Path (Join-Path '${{ github.workspace }}' 'docs-site-build/*') -Destination $resolvedTarget -Recurse -Force
