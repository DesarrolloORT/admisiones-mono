# v5.0.0-14.3.1 — Limpieza de datos autenticados del cliente

## Identificación

- **ID y nivel mínimo:** v5.0.0-14.3.1, L1.
- **Texto oficial:** "Verify that authenticated data is cleared from client storage, such as the browser DOM, after the client or session is terminated. The 'Clear-Site-Data' HTTP response header field may be able to help with this but the client-side should also be able to clear up if the server connection is not available when the session is terminated."
- **Interpretación local:** al terminar la sesión explícitamente, por expiración o por fallo de renovación, el cliente debe eliminar de DOM, memoria y almacenamiento del navegador los datos vinculados a la persona autenticada. La limpieza no puede depender exclusivamente de que el `POST /auth/logout` llegue al servidor.
- **Pregunta:** ¿el frontend elimina los datos autenticados de memoria y de todos los mecanismos de almacenamiento del navegador cuando finaliza la sesión?
- **Avance documental:** DOCUMENTADO.
- **Resultado de seguridad:** FAIL.
- **Fecha de revisión:** 2026-09-24.

## Referencias evaluadas

| Componente | Ruta/repositorio | Commit completo | Cambios locales relevantes / hash | Ambiente |
| --- | --- | --- | --- | --- |
| Monorepo | `C:\GIT\admisiones-mono` | `a599528b61412784a99bab1a6ade82be82f462a1` | `AVANCE.md` y fichas previas sin commit; más cambios ajenos | Revisión estática; runtime no evaluado |
| Frontend | `admisiones` | `86979de2bbe049fdeddeae3dbb263508403dd5d8` | Cambios ajenos amplios; `auth-session.ts` modificado, blob actual `57d7a548dffa973ed038ef626643b39546b6b023`; `enrollment-resume-context.ts` blob actual `94045bbe99a34e761de1c61593cf3bd091839d85` | Revisión estática; runtime no evaluado |
| API | `api-admisiones` | `260667f6025167dcdbe80452443a1a43ca631a65` | Cambios ajenos amplios; `AuthController.cs` blob actual `00a91b5e79c2926a14090f838df181accd5ad479`; `CookieAuthenticationHelper.cs` blob actual `9564390cd6734315b101853dc39f311c57cc62d9` | Revisión estática; runtime no evaluado |
| Core | `api-admisiones/Core` | `01239cdf6054dc5a450dcdaa3a867ae3204b61e7` | Sin inspección de código: no participa en el almacenamiento del navegador observado | No evaluado |

La fuente `OWASP_Application_Security_Verification_Standard_5.0.0_en.json` tiene hash de bytes `DDAAB099D0A85852A81119AC9C8CE4A8616009C5A494A747ED2B3A01CE6032AF`, pero al normalizar sus finales de línea a LF produce el SHA-256 requerido `BCDBEC214D70ABCFAD9284A31D4F9E5134305831D628AAD3AA85D7E26626CB35`.

## Aplicabilidad y alcance

Aplica al frontend Angular: la sesión usa cookies HTTP-only gestionadas por la API y conserva estado de usuario en memoria; además el flujo de inscripciones persiste en `sessionStorage` identificadores obtenidos durante navegación autenticada. La API participa porque borra las cookies al recibir logout.

No se evaluaron runtime, DOM efectivo tras navegación, IndexedDB/caches de navegador, service workers, extensiones, proxy/CDN ni headers HTTP de ambientes desplegados. El inventario estático no halló uso propio de IndexedDB. La telemetría también usa `sessionStorage` para identificador de sesión y traza de rutas; no se clasificó como dato autenticado con la evidencia disponible.

## Evidencia y observaciones

| Ref | Componente | Archivo:línea / símbolo / evidencia | Método y ejecución real | Qué demuestra y qué no |
| --- | --- | --- | --- | --- |
| E1 | Frontend | `src/app/features/auth/services/auth-session.ts:108-124`, `AuthSessionService.clearSession` y `logout` | Lectura estática; no se ejecutaron pruebas | `clearSession` borra `sessionState` y contexto 2FA en memoria. `logout` usa `finalize`, por lo que ejecuta esa limpieza y navega a login aun si falla la llamada de red. No limpia `sessionStorage` ni otros stores. |
| E2 | Frontend | `src/app/core/interceptors/http.ts:96-112, 125-128` | Lectura estática; no se ejecutaron pruebas | ante fallo de refresh o segundo 401 se invoca `clearSession`; la misma limitación de limpieza de almacenamiento persiste. |
| E3 | Frontend | `src/app/features/enrollments/services/enrollment-resume-context.ts:24-31, 43-65, 103-108` | Lectura estática; no se ejecutaron pruebas | guarda en `sessionStorage['inscription-resume-context']` IDs de producto, proceso, ofertas e inscripciones, y solo los quita si alguien llama explícitamente a `EnrollmentResumeContextStore.clear`. |
| E4 | Frontend | `src/app/features/enrollments/services/enrollment-resume-context.ts:15-108`; `AuthSessionService` no inyecta ni llama ese store | Lectura estática y búsqueda de referencias; no se ejecutaron pruebas | no existe vínculo entre logout/expiración y `EnrollmentResumeContextStore.clear`; el dato persiste tras terminar la sesión en la misma pestaña. |
| E5 | Frontend | `src/app/features/auth/services/auth-session.spec.ts:274-293` | Prueba existente leída, no ejecutada | cubre limpieza en memoria tras logout exitoso y con fallo de red. Las aserciones de `localStorage['auth-session']` no prueban limpieza porque el servicio no escribe esa clave; no cubre `inscription-resume-context`, DOM ni `sessionStorage`. |
| E6 | API | `WebApiAdmisiones/WebApiAdmisiones/Controllers/AuthController.cs:302-311`; `Security/Authentication/CookieAuthenticationHelper.cs:158-175` | Lectura estática; no se ejecutaron pruebas ni capturas HTTP | `POST /auth/logout` elimina cookies de access, refresh y activación. No emite `Clear-Site-Data` en el código revisado y el cliente no puede depender de esta respuesta si la red falla. |
| E7 | API | `WebApiAdmisiones/UnitTesting/Controllers/AuthControllerTests.cs:483-497` | Prueba existente leída, no ejecutada | verifica respuesta 200 del controller, pero no inspecciona el encabezado `Set-Cookie` efectivo ni almacenamiento del navegador. |

## Resultado por componente y conclusión

- **Frontend: FAIL.** Hay limpieza resiliente del estado de autenticación en memoria, pero el flujo autenticado conserva en `sessionStorage` IDs de inscripciones y no hay llamada de logout, expiración o fallo de refresh que los elimine. Esto contradice el requisito de limpiar datos autenticados del almacenamiento del cliente al terminar la sesión.
- **API: evidencia parcial.** El endpoint de logout borra cookies, lo que contribuye a terminar la sesión, pero no puede limpiar `sessionStorage` y no se observó `Clear-Site-Data`.
- **Core: fuera del flujo inspeccionado.**

La conclusión es FAIL por la evidencia estática concreta de persistencia del contexto de reanudación. No se afirma que todos los demás datos del cliente estén correctamente inventariados o limpiados: faltan verificaciones de runtime y de infraestructura.

## Hallazgos y propuesta documental

1. **Contexto de reanudación persiste tras el cierre de sesión.**
   - Condición e impacto: `inscription-resume-context` contiene IDs ligados a una sesión autenticada y continúa disponible en la pestaña después de logout, expiración o caída de red; puede revelar contexto del flujo al siguiente usuario de ese perfil del navegador.
   - Evidencia: E1-E4.
   - Responsable candidato: frontend `admisiones`; coordinación con API para cobertura de cookies/headers.
   - Propuesta para una fase de fixes: centralizar una rutina de cierre de sesión que invoque la limpieza del contexto de reanudación y de cualquier store autenticado; cubrir logout exitoso, fallo de red y expiración. Evaluar `Clear-Site-Data` como defensa complementaria sin sustituir la limpieza local.
   - Validación futura: prueba de navegador que siembre el contexto, ejecute cada terminación de sesión y confirme la ausencia de las claves, del DOM protegido y de cookies; inspeccionar headers de ambiente real.

No se distribuyó propuesta a las ramas `fix/owasp`: esta sesión no modifica código, no se verificó un worktree aplicable y la propuesta queda centralizada aquí.

## Pendientes y dependencias

- El equipo frontend debe aportar o ejecutar pruebas de navegador para logout, refresh fallido y 401, comprobando `sessionStorage`, `localStorage`, IndexedDB, Cache Storage, DOM y navegación.
- El equipo de plataforma/API debe aportar una captura HTTP del logout en cada ambiente y confirmar si proxy/CDN agrega `Clear-Site-Data` u otro mecanismo de limpieza; no se infiere desde el código.
- El equipo funcional debe clasificar los datos de telemetría persistidos en `sessionStorage` y confirmar si el `sessionId` o la traza de rutas deben limpiarse junto con la sesión.
- La remediación debe inventariar todos los stores de features antes de declarar PASS; este punto se relaciona con v5.0.0-14.3.2 y v5.0.0-14.3.3 (L2), sin adelantarlos.

## Checkpoint para retomar

Se validó la fuente ASVS sobre LF normalizado, se leyeron las instrucciones locales, se intentó CodeGraph en raíz y frontend (sin índice disponible) y se realizó revisión estática acotada. La ficha está completa y el requisito queda DOCUMENTADO / FAIL. No se ejecutaron tests, builds, runtime, capturas HTTP, scans, commits ni pushs.

Con un nuevo pedido, pasar únicamente a `v5.0.0-15.1.1` (L1), verificar Git/fuente y fijar referencias antes de investigar.