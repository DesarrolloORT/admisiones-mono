# v5.0.0-12.1.1 — Versiones TLS recomendadas

## Identificación

- **ID y nivel mínimo:** v5.0.0-12.1.1, L1.
- **Texto oficial:** “Verify that only the latest recommended versions of the TLS protocol are enabled, such as TLS 1.2 and TLS 1.3. The latest version of the TLS protocol must be the preferred option.”
- **Interpretación local:** comprobar que las superficies TLS propias del frontend y de la API no habiliten protocolos anteriores a TLS 1.2 y que TLS 1.3 pueda preferirse cuando la plataforma lo soporte.
- **Pregunta:** ¿las terminaciones TLS que sirven el frontend y la API habilitan únicamente TLS 1.2/TLS 1.3, con TLS 1.3 preferido donde esté disponible?
- **Avance documental / resultado:** DOCUMENTADO / NEEDS_REVIEW.
- **Fecha de revisión:** 2026-09-23.

## Referencias evaluadas

| Componente | Ruta/repositorio | Commit completo | Cambios locales relevantes / hash | Ambiente |
| --- | --- | --- | --- | --- |
| Monorepo | `C:\GIT\admisiones-mono` | `25ee63c90740bb9b8b73349c85a71bbce579b446` | `security/asvs/planning/AVANCE.md` ya estaba modificado y hay archivos ajenos sin seguimiento | revisión estática; runtime no evaluado |
| Frontend | `admisiones` | `86979de2bbe049fdeddeae3dbb263508403dd5d8` | worktree con cambios ajenos amplios; `src/environments/environment.generated.ts` fue sólo leído | revisión estática; hosting no evaluado |
| API | `api-admisiones` | `260667f6025167dcdbe80452443a1a43ca631a65` | existe `WebApiAdmisiones/dotnet-tools.json` sin seguimiento; `WebApiAdmisiones/Program.cs` (`395176975F957875E99C94DE9A8194EDC53C0C27ED0B8D6A7380C7D8FA73E5BC`) y `Extensions/MiddlewarePipelineExtensions.cs` (`CD34AD7F687B86402F739531C23E1696E5903342EB2541B58FC027A48FC24AC2`) ya estaban modificados al leerlos | revisión estática; runtime no evaluado |
| Core consumido por API | `api-admisiones/Core` | `01239cdf6054dc5a450dcdaa3a867ae3204b61e7` | no se localizó configuración de servidor TLS propia para este requisito | revisión estática; runtime no evaluado |

La fuente `security/asvs/source/OWASP_Application_Security_Verification_Standard_5.0.0_en.json` conserva el SHA-256 esperado `BCDBEC214D70ABCFAD9284A31D4F9E5134305831D628AAD3AA85D7E26626CB35` al normalizar CRLF a LF. No existe `.codegraph/` en el monorepo, por lo que se usó `rg` antes de leer código.

## Aplicabilidad y alcance

Aplica a la API ASP.NET Core, que configura Kestrel y recibe tráfico HTTPS, y al frontend, cuyo origen de API configurado usa HTTPS y cuyo hosting es una terminación TLS independiente. También aplica a los reverse proxies, balanceadores, CDN, IIS o plataforma que pudieran terminar TLS antes de Kestrel; ninguno está versionado en este árbol.

El alcance cubre el arranque, pipeline y configuración versionada de la API, la configuración fuente del frontend y el inventario de manifiestos de despliegue. No cubre certificados, listeners y políticas efectivas de cada ambiente, negociación con clientes, orden de preferencia observado, configuraciones del sistema operativo/contenedor ni servicios externos. Las URL HTTP de LDAP, OTLP y algunos servicios internos observadas en `appsettings*` son relevantes para V12.2/V12.3, pero no determinan qué versiones TLS se habilitan en el servidor bajo este requisito.

## Evidencia y observaciones

| Ref | Componente | Archivo:línea / símbolo / evidencia | Método y ejecución real | Qué demuestra y qué no |
| --- | --- | --- | --- | --- |
| E1 | API | `WebApiAdmisiones/Extensions/KestrelExtensions.cs:17-26`, `ConfigureKestrelSecurity` | lectura estática; no ejecutada | `ConfigureHttpsDefaults` restringe `listenOptions.SslProtocols` a `Tls12 | Tls13`; descarta TLS 1.0/1.1 para los endpoints HTTPS que efectivamente gestione Kestrel, pero no demuestra listener, certificado ni la negociación real. |
| E2 | API | `WebApiAdmisiones/Program.cs:37`, llamada a `builder.WebHost.ConfigureKestrelSecurity()` | lectura estática; no ejecutada | el control E1 forma parte del arranque de la aplicación; no descarta una terminación TLS previa que use otra política. |
| E3 | API | `WebApiAdmisiones/Extensions/MiddlewarePipelineExtensions.cs:22-42` | lectura estática; no ejecutada | configura HSTS fuera de Development, procesa `X-Forwarded-Proto` y aplica `UseHttpsRedirection`; ayuda a operar detrás de proxy y redirigir HTTP, pero no configura ni prueba las versiones TLS de ese proxy. |
| E4 | API | `WebApiAdmisiones/WebApiAdmisiones.csproj:3` (`net10.0`) y `Dockerfile:49-71` (imagen runtime ASP.NET 10, puertos 8080/8081) | lectura estática; no ejecutada | la API usa una plataforma moderna y el contenedor no declara un listener/certificado TLS; no establece qué protocolo expone el despliegue ni qué puerto termina TLS. |
| E5 | Frontend | `admisiones/src/environments/environment.generated.ts:4` | lectura estática; no ejecutada | el URL configurado para la API es `https://apiadmisionesdesa.ort.edu.uy`; no prueba el hosting del frontend, la configuración TLS de esa API ni artefactos/configuración por ambiente. |
| E6 | Monorepo | inventario con `rg --files` de Kubernetes, Helm, ingress, Nginx, Traefik, IIS, Terraform, Bicep, pipelines y compose; sólo aparecen archivos de planificación de seguridad | búsqueda estática; no ejecutada | no se localizó infraestructura versionada que configure terminación TLS o su orden de cifrados/protocolos; no demuestra que esa infraestructura no exista fuera del monorepo. |

No se localizaron `SslProtocols` adicionales, `ConfigureKestrel` alternativos ni una configuración Kestrel declarativa que amplíe el conjunto de E1 dentro del código fuente revisado. La unión `Tls12 | Tls13` expresa protocolos permitidos, pero no permite inferir, sin handshake o documentación de runtime, cuál será el preferido por el SO, Kestrel y el cliente.

## Resultado por componente y conclusión

- **API — NEEDS_REVIEW:** la configuración fuente de Kestrel está alineada con TLS 1.2 y TLS 1.3, y el pipeline fuerza HTTPS a nivel de aplicación. Falta evidencia determinística de los listeners activos, las terminaciones TLS previas y la preferencia real de TLS 1.3 por ambiente.
- **Frontend — NEEDS_REVIEW:** el consumidor configura un origen API HTTPS, pero el repositorio no contiene la configuración de la terminación TLS que sirve su aplicación ni una prueba de protocolo contra el sitio publicado.
- **Core — NEEDS_REVIEW:** no se identificó una terminación TLS propia; sus consumidores y conexiones salientes se revisarán en los requisitos de comunicación correspondientes. Esto no acredita el control compartido de los bordes públicos.

La conclusión global es **NEEDS_REVIEW**, de acuerdo con ADR-002: la evidencia estática sustenta una restricción de Kestrel, pero no verifica de forma reproducible todas las superficies TLS ni la preferencia efectiva de TLS 1.3.

## Hallazgos y propuesta documental

No se confirma un protocolo TLS obsoleto habilitado en el código propio inspeccionado. Se registra una brecha de evidencia: no hay configuración de reverse proxy/hosting ni resultados de handshake por ambiente en el monorepo, por lo cual un cambio externo podría habilitar TLS 1.0/1.1 o impedir la preferencia de TLS 1.3 sin que E1 lo detecte. Responsable candidato: plataforma/infraestructura, en coordinación con API y frontend. En una fase posterior, inventariar todas las terminaciones TLS, fijar explícitamente la política donde corresponda y validar con clientes que ofrezcan TLS 1.2 y TLS 1.3. No se aplicó ningún cambio.

## Pendientes y dependencias

1. Plataforma debe aportar la configuración efectiva y el inventario de terminaciones TLS (proxy, balanceador, CDN, IIS/hosting y Kestrel) para Production, Preproduction, Testing y Development relevantes.
2. Ejecutar posteriormente, sin secretos ni PII, una prueba de handshake por hostname/puerto con clientes que ofrezcan TLS 1.0, 1.1, 1.2 y 1.3; registrar que 1.0/1.1 fallan y que TLS 1.3 se negocia/puede preferirse cuando está soportado.
3. API debe confirmar si Kestrel termina TLS en algún ambiente o si recibe siempre tráfico ya terminado por un proxy; si hay puertos HTTP internos intencionales, documentar su boundary y red aislada.
4. Frontend/plataforma debe aportar la configuración TLS de su hosting. El `web.dev.config` de la documentación no acredita ni reemplaza la configuración de entrega de la aplicación Angular.
5. Las conexiones HTTP a LDAP, OTLP y servicios internos observadas en la API se evaluarán, sin reabrir este ID, bajo `v5.0.0-12.2.1`, `v5.0.0-12.2.2` y los requisitos V12.3 aplicables.

## Checkpoint para retomar

Se leyó la fuente oficial, catálogo, planes de frontend/API/Core, instrucciones locales y configuración fuente acotada de Kestrel, pipeline, contenedor y frontend. No se ejecutaron tests, builds, runtime, handshakes, scans, regeneración de contratos/Devart, fixes, commits ni pushs. Esta ficha queda completa: el siguiente pedido debe avanzar únicamente a `v5.0.0-12.2.1` (L1), preservando los pendientes de infraestructura sin reabrir este requisito salvo nueva evidencia.
